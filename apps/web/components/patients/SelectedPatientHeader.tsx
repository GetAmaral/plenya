"use client";

/**
 * Deprecated: patient context is shown in the sticky PatientContextBar
 * in the authenticated layout. This component now returns null so all
 * existing pages continue to compile without changes.
 */
export function SelectedPatientHeader() {
  return null;
}
