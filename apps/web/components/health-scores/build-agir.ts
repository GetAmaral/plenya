// buildAgir — fonte ÚNICA compartilhada em @plenya/ui (web + site). Re-export para os
// imports existentes do EMR (`./build-agir`) continuarem válidos.
export { buildAgir } from '@plenya/ui/score-radar'
export type { AgirRadarData } from '@plenya/ui/score-radar'
