{
  "name": "@plenya/emails",
  "version": "1.0.0",
  "private": true,
  "description": "Plenya email templates (React Email pre-rendered to static HTML).",
  "scripts": {
    "build": "tsx scripts/build.ts",
    "clean": "rm -rf dist/*.html"
  },
  "dependencies": {
    "@react-email/components": "^0.0.30",
    "@react-email/render": "^1.0.3",
    "react": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
