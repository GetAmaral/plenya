{
  "name": "@plenya/ui",
  "version": "1.0.0",
  "private": true,
  "description": "Componentes React compartilhados entre EMR (apps/web) e site (apps/site). Começa pelo radar de escore AGIR (fonte única de geração).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./score-radar": "./src/score-radar/index.ts"
  },
  "scripts": {
    "lint": "eslint . --ext .ts,.tsx",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "react": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "react": "catalog:",
    "typescript": "catalog:"
  }
}
