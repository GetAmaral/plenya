preciso que voce abasteça o banco de dados com os dados de escore.csv - analise o arquivo e decida qual a melhor forma de preencher o banco de dados - pela leitura direta ou via um script especifico que voce crie. 
as colunas docsv estão em ordem
- coluna 1 = grupo (só a primeira linha que tem o grupo fica ativa, as seguintes ficam nulas o que significa que todas as linhas são do mesmo
  grupo até o próximo nome de grupo aparecer)
- a mesma lógica vale para a coluna 2 = subgrupo
- a coluna 3 = item é diferente, se estiver preenchida é o item ele mesmo, sem parentItem, já olhe a coluna 5 = points (se tiver valor, preencha o campo points do
  item)
- se a coluna 3 = item estiver nula, e a próxima coluna 4 = subitem estiver preenchida, use a mesma logica da coluna 3 para preencher o item, mas com parentItem sendo o ultimo item que teve valor na coluna 3 = item
- agora se ambas colunas 3 e 4 estiverem preenchidas, o item é um exame, logo o nome do item é a coluna 3 e a coluna 4 é a unit e unitConversion do item -- é um string que deve ser explodido pelo texto " | ", array[0] é a unit e array[1] é a unitConversion (se houver)
- para cada item (coluna 3 ou 4 preenchida), se tiver points > 0 na coluna 5, verifique as colunas seguintes (coluna 6 = nivel 0, coluna 7 = nivel 1, etc até a
  coluna 11 = nivel 5) -- para cada coluna que estiver preenchida, vamos criar um objeto escoreLevel filho do item em questão na
  linha atual - preencha corretamente o level com level correto (0 até 5), definition com o texto exato escrito na coluna, daí precisa interpretar o texto para preencher 
corretamente lowerLimit e upperLimit:
 -- se for um valor de textosapenas, ignore lowerLimit e upperLimit
 -- se for um valor de X a Y ou X-Y ou X - Y, onde X e Y são numeros, preencha lowerLimit com X e upperLimit com Y
 -- se for um valor como <X ou <=X, preencha upperLimit com X e deixe lowerLimit em branco
 -- se for um valor como >X ou >=X, preencha lowerLimit com X e deixe upperLimit em branco
** as vezes os valores numericos decimais podem usar tanto "." quanto "," para divisor decimal -- normalize para "." ao passar para o banco de dados. nenhum valor utiliza separador de milhar

Antes de iniciar, analise e estude o assunto e me traga seu plano de ação para eu aprovar